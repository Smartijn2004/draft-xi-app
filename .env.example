# Where feedback submissions get delivered.
# Easiest setup: create a Discord server → channel settings → Integrations →
# Webhooks → New Webhook → copy the URL and paste it here.
# Slack incoming webhooks (hooks.slack.com) are also supported.
# If unset, feedback is logged to the server console instead.
FEEDBACK_WEBHOOK_URL=
